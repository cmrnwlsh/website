version: '3'

services:
  proxy:
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    restart: unless-stopped
    volumes:
      - $HOME/.local/nginx/conf.d/:/etc/nginx/conf.d/:ro
      - $HOME/.local/www/certbot/:/var/www/certbot/:ro
  certbot:
    image: certbot/certbot:latest
    volumes:
      - /home/server/.local/www/certbot/:/var/www/certbot/:rw
      - /home/server/.local/letsencrypt/:/etc/letsencrypt/:rw
